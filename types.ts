export interface ExpenseType {
  id: string;
  name: string;
  amount: string;
  percentage: string;
}

export interface SpendingType {
  id: number;
  category: string;
  amount: number;
  date: string;
}